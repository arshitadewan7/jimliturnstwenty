// Cute little confetti drop when the page loads
window.addEventListener('load', () => {
  console.log("Welcome to Jimli's birthday site 🎉");
  // You can later add animation libraries here like confetti.js or GSAP
});
